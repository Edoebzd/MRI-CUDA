#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <vector>

using namespace std;

int next_power_of_two(int N);
void write_to_png(vector<vector<float>> data, string outfile);

#endif //UTILS_H